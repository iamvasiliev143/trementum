# Project Setup with Docker

---

## 1️⃣ Install Docker
Before getting started, you need to install **Docker** and **Docker Compose**.

[Download Docker Desktop](https://www.docker.com/products/docker-desktop)

---

## 2️⃣ Create an Environment File
In the root of your project, create a `.env` file based on the example:

```bash
cp .env.example .env
